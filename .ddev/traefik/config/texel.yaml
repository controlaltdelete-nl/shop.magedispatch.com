#ddev-generated
# If you remove the ddev-generated line above you
# are responsible for maintaining this file. DDEV will not then
# update it, for example if you add `additional_hostnames`, etc.

http:
  routers:
    texel-web-80-http:
      entrypoints:
        - http-80
      rule: HostRegexp(`^texel\.test$`)
      
      service: "texel-web-80"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "texel-redirectHttps"
    texel-web-8025-http:
      entrypoints:
        - http-8025
      rule: HostRegexp(`^texel\.test$`)
      
      service: "texel-web-8025"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "texel-redirectHttps"
    texel-elasticsearch-9200-http:
      entrypoints:
        - http-9200
      rule: HostRegexp(`^texel\.test$`)
      
      service: "texel-elasticsearch-9200"
      ruleSyntax: v3
      tls: false
      # middlewares:
      #   - "texel-redirectHttps"
    
    
    
    texel-web-80-https:
      entrypoints:
        - http-443
      rule: HostRegexp(`^texel\.test$`)
      
      service: "texel-web-80"
      ruleSyntax: v3
      
      tls: true
      
    texel-web-8025-https:
      entrypoints:
        - http-8026
      rule: HostRegexp(`^texel\.test$`)
      
      service: "texel-web-8025"
      ruleSyntax: v3
      
      tls: true
      
    
    texel-elasticsearch-9200-https:
      entrypoints:
        - http-9201
      rule: HostRegexp(`^texel\.test$`)
      
      service: "texel-elasticsearch-9200"
      ruleSyntax: v3
      
      tls: true
      
    

  middlewares:
    texel-redirectHttps:
      redirectScheme:
        scheme: https
        permanent: true

  services:
    texel-web-80:
      loadbalancer:
        servers:
          - url: http://ddev-texel-web:80
        
    texel-web-8025:
      loadbalancer:
        servers:
          - url: http://ddev-texel-web:8025
        
    
    
    texel-elasticsearch-9200:
      loadbalancer:
        servers:
          - url: http://ddev-texel-elasticsearch:9200
        
    
    

tls:
  certificates:
    - certFile: /mnt/ddev-global-cache/traefik/certs/texel.crt
      keyFile: /mnt/ddev-global-cache/traefik/certs/texel.key